
Процедура ПодпискаНаСобытие1ПриЗаписи(Источник, Отказ) Экспорт

	тЗапись = Новый ЗаписьJSON;
	тЗапись.УстановитьСтроку();

	СериализаторXDTO.ЗаписатьJSON(тЗапись, Источник, НазначениеТипаXML.Явное);
	СериализованнаяСтрока = тЗапись.Закрыть();


	Соединение = Новый HTTPСоединение("localhost", 8080,,,,Ложь);

	Сообщить(СериализованнаяСтрока);

	Запрос = Новый HTTPЗапрос("camel/api/student");
	Запрос.Заголовки.Вставить("Content-type", "application/json");

	Запрос.УстановитьТелоИзСтроки(СериализованнаяСтрока);

	Ответ = Соединение.ВызватьHTTPМетод("POST", Запрос);

	ОтветВВидеСтроки = Ответ.ПолучитьТелоКакСтроку("UTF-8");

	Сообщить(ОтветВВидеСтроки);
КонецПроцедуры
